machine:
  node:
    version: "6.8.0"
  timezone: Asia/Tokyo
dependencies:
  override:
    - yarn install
test:
  override:
    - npm run test:rich
    - npm run test:including-babel
    - npm run flow
    - npm run lint
